<h1>Gossips#index</h1>
<p>Find me in app/views/gossips/index.html.erb</p>

 <% Gossip.all.each do |gossip| %>
        <p>
          <%= gossip.title %><br/>
          <%= gossip.user.first_name %><br/>
          <%= link_to "test", gossips_path(gossip) %>
        </p>
      <% end %>