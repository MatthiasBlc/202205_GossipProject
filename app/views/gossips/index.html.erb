<h1>Gossips#index</h1>
<p>Find me in app/views/gossips/index.html.erb</p>

 <% @gossips.each do |gossip| %>
        <p>
          <%= gossip.title %><br/>
          <%= gossip.user.first_name %><br/>
   <p><%= link_to 'Voir le gossip', gossip_path(gossip) %></p>
        </p>
      <% end %>